{
  "name": "pi-plan-wizard",
  "description": "Plan and implement projects. Use /plan to create plans, /implement to execute them as a step-tracking loop with per-phase fresh sessions, a shared notes scratchpad, blocked-step handling, and no context compaction.",
  "version": "3.0.0",
  "type": "module",
  "keywords": ["pi-package"],
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "pi": {
    "extensions": ["./index.ts"]
  }
}